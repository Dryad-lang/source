// tests/user/scripts/29_system_modules_static.dryad
// EXPECTED: current_user
// Teste: Simulação de System modules com métodos estáticos

class Environment {
    public static fun getEnv(key, defaultValue) {
        return defaultValue;  // Simula retorno do valor padrão
    }
}

// Usa método estático de Environment
let user = Environment.getEnv("USER", "current_user");
print(user);
